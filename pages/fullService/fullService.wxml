<!--pages/fullService/fullService.wxml-->
<view class="container">
  <view class="usermotto">
    <button type="default" size="{{primarySize}}" plain="{{plain}}" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toPS"> {{ps}} </button>
  </view>
</view>
<view class="container">
  <view class="usermotto">
    <button type="default" size="{{primarySize}}" plain="{{plain}}" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toSupplement"> {{supplement}} </button>
  </view>
</view>
