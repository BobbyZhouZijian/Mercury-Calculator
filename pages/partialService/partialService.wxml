<!--pages/partialService/partialService.wxml-->
<view class="page">
  <view class="container">
    <text>请选择您当前的文书进度：</text>
    <view class="div-line"></view>
    <text>\n\n\n</text>
    <button class="btn" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toIdea"> {{idea}} </button>
    <text>\n\n\n</text>
    <button class="btn" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toOutline"> {{outline}} </button>
    <text>\n\n\n</text>
    <button class="btn" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toDraft"> {{draft}} </button>
    <text>\n\n\n</text>
      <button class="btn" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toFullScript"> {{fullScript}} </button>
  </view>
  <view class="weui-footer">
    <view class="fix">
      <view style="color: black">
          Mercury Studio
      </view>
      <view class="weui-footer__text">Copyright © 2020: all rights reserved</view>
    </view>
  </view>
</view>
<image class="blackImage" src="../../images/business.jpg"> </image>

